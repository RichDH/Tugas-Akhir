import 'package:flutter/material.dart';

class SearchExploreScreen extends StatelessWidget {
  const SearchExploreScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Explore & Search')),
      body: const Center(child: Text('Halaman Explore & Search (UI akan dibuat nanti)')),
    );
  }
}