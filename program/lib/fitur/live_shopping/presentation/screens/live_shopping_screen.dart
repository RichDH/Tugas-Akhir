import 'package:flutter/material.dart';

class LiveShoppingScreen extends StatelessWidget {
  const LiveShoppingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Live Shopping')),
      body: const Center(child: Text('Halaman Live Shopping (UI akan dibuat nanti)')),
    );
  }
}